type Person {
    id: ID!,
    name: String,
    age: Int
}

type Bet {
    horse: String!
    amount: Int!
    timestamp: String!
}

type Query{
    getAll: [Person]
    getOne(id: Int): Person
    getByDataLoader(ids:[Int]): [Person]
}

type Mutation{
    createPerson(name: String!, age: Int!): Person
}

type Subscription {
    bet(horse: String!): Bet
}
